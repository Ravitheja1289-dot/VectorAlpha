# Vector Alpha Dashboard - Modular Streamlit App
# Minimal dependencies for institutional-grade research dashboard

# Core framework
streamlit>=1.28.0              # Web framework for dashboards

# Data manipulation
pandas>=1.5.0                  # DataFrames, read_parquet
numpy>=1.24.0                  # Numerical arrays (required by pandas)
pyarrow>=13.0.0               # Fast Parquet I/O

# Visualization
plotly>=5.0.0                  # Interactive charts (lighter than matplotlib)

# Optional: For extended functionality (not required for base dashboard)
# scikit-learn>=1.3.0          # ML algorithms (if adding portfolio optimization)
# scipy>=1.11.0                # Scientific computing (if adding statistical tests)
